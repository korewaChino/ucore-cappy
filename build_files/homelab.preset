# Cappy's Homelab systemd preset

disable firewalld.service
enable cockpit.service
enable multipathd.service
enable avahi-daemon.*
enable bootc-fetch-apply-updates.timer
# in systemd we trust
disable chronyd
enable systemd-timesyncd
